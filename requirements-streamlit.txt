# requirements-streamlit.txt
# Used by Streamlit Community Cloud deployment.
# The app loads pre-computed parquet/json files only — no model inference.

streamlit>=1.36
pandas>=2.2,<3.0
pyarrow>=16
matplotlib>=3.9
numpy>=1.26,<3.0
